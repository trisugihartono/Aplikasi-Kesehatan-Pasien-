<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ac.id.atmaluhur.kesehatan">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kesehatan">
        <activity android:name=".InfoAplikasiActivity"></activity>
        <activity android:name=".TentangApkActivity" />
        <activity android:name=".DoctorActivity" />
        <activity android:name=".SplashScreenAct" />
        <activity android:name=".HealthStoreActivity" />
        <activity android:name=".SuccessRegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterTwoActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>